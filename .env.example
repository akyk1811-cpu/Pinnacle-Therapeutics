# Public site URL, added when the domain is purchased
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Configure a server-side email provider before enabling contact delivery.
# Never commit real credentials.
CONTACT_WEBHOOK_URL=
